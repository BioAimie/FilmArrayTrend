SET NOCOUNT ON
SELECT DISTINCT
	[Interpretation]
FROM [FADataWarehouse].[dbo].[SummarizedPositiveAssayResults] S WITH (NOLOCK) INNER JOIN [FADataWarehouse].[dbo].[RunData] R WITH(NOLOCK) 
	ON S.[RunDataId] = R.[RunDataId]
WHERE S.[ResultType] NOT LIKE 'Control' AND R.[PouchTitle] LIKE '%Resp%'

